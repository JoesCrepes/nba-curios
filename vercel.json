{
  "buildCommand": "(pip install -r projects/nba-two-point-percentage/scripts/requirements.txt && python projects/nba-two-point-percentage/scripts/fetch_data.py --update-current || echo 'ETL skipped — deploying with existing data') && npm install && npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/season-trajectories",
      "destination": "/season-trajectories/index.html"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
